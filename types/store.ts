export interface StoreCreationProps {
    storeName: string;
    accountManager: string;
    storeAddress: string;
    mainClientContact: string;
    mainClientContactNumber: string;
    StoreCode: string;
    startDate: string;
    endDate: string;
  }
  